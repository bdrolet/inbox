# Toolchain pinned exactly. CI installs only this file, so a floating range
# lets any upstream release turn CI red with no repo change — which is exactly
# how ruff 0.16.x did it, by starting to format Python code blocks inside
# Markdown. Bump these deliberately (a ruff bump wants a format run).
ruff==0.16.3
mypy==2.3.1
pytest==9.1.1
pre-commit>=3.7
httpx>=0.27
# Runtime deps the test suite imports (CI installs only this file):
requests>=2.28.0
msal>=1.24.0
python-dotenv>=1.0.0
fastapi>=0.111
tzdata>=2024.1  # services.sweep_rules imports zoneinfo at module load
anthropic>=0.25
google-cloud-pubsub>=2.18.0
opentelemetry-sdk>=1.24
opentelemetry-exporter-otlp-proto-http>=1.24
psycopg[binary]>=3.1  # tests/test_pipeline_repair.py imports the real repo/ modules
