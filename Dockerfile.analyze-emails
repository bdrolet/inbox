FROM python:3.11-slim

WORKDIR /app

# So `from services...` works when running scripts/analyze_emails.py
ENV PYTHONPATH=/app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "-u", "scripts/analyze_emails.py"]
